import express from 'express';
const subgroupRouter = express.Router();

subgroupRouter.get('/:id', () => {});
subgroupRouter.post('/', () => {});
subgroupRouter.put('/:id', () => {});
subgroupRouter.delete

export default subgroupRouter;