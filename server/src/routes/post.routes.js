import { Router } from 'express';
const postRouter = Router();

postRouter.get('/:id', () => {});
postRouter.post('/', () => {});
postRouter.put('/:id', () => {});
postRouter.delete('/:id', () => {});

export default postRouter;